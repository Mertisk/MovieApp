@model Movie

    <h1>
        Edit Movie
    </h1>

    <hr />


    <form asp-controller="Movies" asp-action="Edit" method="post">
        @*name ismi önemli burada movieId ya da MovieId olmak zorunda yoksa hata alırsın*@
        @*Bu gizli alanın görevi, sunucuya, formun gönderildiği zaman hangi film üzerinde düzenleme yapıldığını belirtmektir. Örneğin, bir film düzenleme formu gösterildiğinde, formun içinde gizli bir MovieId alanı yer alır ve bu alan, düzenlenen filmin kimliğini taşır. Sunucu, form gönderildiğinde bu gizli alanı kullanarak hangi filmi güncelleyeceğini belirler.*@
        <input type="hidden" name="MovieId" value="@Model.MovieId" />
        <div class="form-group">
            ,<label asp-for="Title"></label>
            <input asp-for="Title" class="form-control" value="@Model.Title" />
            <span class="text-danger" asp-validation-for="Title"></span>

        </div>
        <div class="form-group">
            <label asp-for="Description"></label>
            <textarea asp-for="Description" class="form-control">@Model.Description</textarea>
            <span class="text-danger" asp-validation-for="Description"></span>


        </div>
       
        <div class="form-group">
            <label asp-for="ImageUrl"></label>
            <input asp-for="ImageUrl" class="form-control" value="@Model.ImageUrl" />
            <span class="text-danger" asp-validation-for="ImageUrl"></span>


        </div>
        <div class="form-group">
            <label asp-for="GenreId"></label>
            <select asp-for="GenreId" asp-items="@ViewBag.Genres" class="form-control">
                @*disabled seçiniz alanını seçemez*@
                <option selected disabled>Seçiniz</option>
                @*<option value="1">Macera</option>
                <option value="2">Komedi</option>
                <option value="3">Romantik</option>
                <option value="4">Savaş</option>*@
            </select>
        </div>
        <button type="submit" class="btn btn-primary">Submit</button>
    </form>